---
title: 代码地图（CODEMAPS 综述）
category: source
tags:
  - i18n-manager
  - 代码地图
  - CODEMAPS
source:
[[]]"
[[]]"
[[]]"
[[]]"
[[]]"
[[]]"
created: 2026-08-19
updated: 2026-08-19
aliases:
  - CODEMAPS
  - Codemaps
---

# 代码地图（CODEMAPS 综述）

> 5 份代码地图源文档的职责综述与共同入口点。代码地图描述的是 2026-08-05 的实现快照，与当前状态存在多处出入。

> [!warning] ⚠️ 文档矛盾
> 全部 CODEMAPS 生成于 **2026-08-05**，**早于** 2026-08-09 键级锁定移除。architecture/backend/data/frontend 中的键级锁、`lock`/`unlock` 事件、`LOCK_TIMEOUT`、`LockIndicator`、扁平 `SchemaObject`、`flattenObject` 遇数组抛错、手动光标恢复均已过时。
[[]]` §6。

## 各份职责

| 文档 | 覆盖范围 | 要点 |
|------|---------|------|
[[]] | 整套索引 | 5 份地图目录 + 入口点 + 关键技术 + 数据存储 |
[[]] | 系统 | 系统分层图、6 个关键架构决策、入口点 |
[[]] | 服务端 | REST 路由表、数据层模块、Socket.IO 事件、`ApiResponse` 信封、Zod 校验 |
[[]] | 持久化 | 文件结构、类型定义、扁平化转换管道、并发保证 |
[[]] | 客户端 | 页面、组件清单、hooks、Zustand stores、数据流 |
[[]] | 外部 | 依赖表、环境变量、零外部服务、版本说明 |

## 共同入口点

- **Server**：`server.ts`（Express 5 + Socket.IO + Next.js）
- **Home Page**：`src/app/page.tsx`
- **Editor Page**：`src/app/projects/[id]/page.tsx`
- **Data Layer**：`src/lib/data-layer/index.ts`
- **Socket Handler**：`src/lib/socket-handler.ts`

## 6 个关键架构决策（architecture.md）

[[]]）
2. **Socket.IO 实时协作**：按项目房间隔离；⚠️ 原含键级锁定（30s 超时）已移除
3. **编辑器 Monaco only**：左右双栏均用 `@monaco-editor/react`；导入预览用 DiffEditor
4. **自动保存管道**：RxJS debounce → 哈希去重 → Socket.IO 持久化 → 原子写 → 广播
5. **数据流方向**：onChange → Subject → debounce → parse → store → Socket.IO → 服务端原子写
6. **Schema 变更传播**：计算增删改 → `applyLocaleSync`（客户端）+ `syncSchemaChangesToLocales`（服务端）→ 广播

## 使用建议

[[]][[]]。

## 关联

[[]] — 16 份源文档完整清单
[[]]
[[]]
[[]]
[[]]
